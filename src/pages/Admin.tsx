import { Layout } from '@arco-design/web-react';

const Content = Layout.Content;

export function Admin() {
    return (
        <Layout>
            <Content>
                <h1>Admin</h1>
            </Content>
        </Layout>
    );
}